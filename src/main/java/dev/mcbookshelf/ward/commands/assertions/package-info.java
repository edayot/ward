/**
 * The assert/await conditions: one class per condition, each owning its command nodes, execution
 * logic and messages, registered through {@link dev.mcbookshelf.ward.commands.assertions.Assertions}.
 */
package dev.mcbookshelf.ward.commands.assertions;
